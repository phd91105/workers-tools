export type FshareAccount = {
  user_email?: string;
  password?: string;
};

export type FshareFile = {
  url?: string;
  password?: string;
};

export type FshareAuth = {
  token?: string;
  session_id?: string;
};
